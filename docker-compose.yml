services:
  user-center:
    image: kouleen/system-center:latest
    container_name: system-center
    environment:
      - ETCD_ENDPOINTS=${ETCD_ENDPOINTS}
      - ADDRESS=${ADDRESS}
      - REDIS_ADDR=${REDIS_ADDR}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - REDIS_DB=${REDIS_DB}
    networks:
      - go-server-internal
networks:
  go-server-internal:
    external: true
    name: go-bootstrap-internal

